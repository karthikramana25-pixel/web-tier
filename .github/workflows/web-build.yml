name: Build & Deploy React Web Tier

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Install dependencies
        working-directory: web-tier
        run: npm install

      - name: Build React app
        working-directory: web-tier
        run: npm run build

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: AKIAUYVWA6VUAKCXKPLW
          aws-secret-access-key: 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
          aws-region: us-east-1

      - name: Upload build to S3
        run: |
          aws s3 sync web-tier/build/ s3://threetierkar/web-build/ --delete
